
import React from 'react';
import { UI_THEME } from '../constants/ui_designs';

interface UpdatePopupProps {
    apkUrl: string | null;
}

export const UpdatePopup: React.FC<UpdatePopupProps> = () => {
    // SYSTEM OVERRIDE: Update prompt disabled as per user request
    return null;
};
